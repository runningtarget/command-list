#Unix/Linux/MacOSX Commands
* `~` : This symbol means "home" on the UNIX command line.
* `/` : This symbol means "root", the ultimate parent directory.
* `whoami` : This makes the terminal output the current user name.
* `pwd` : 
* `cd` : change directory
* `ls` :  list files
* `cd ..` :  change direction (forgot ..)
* `mkdir` : make directory
* `touch` : change file timestamp
* `cat` : link files and print on the standard output
* `.` :
* `rm` : remove file
* `rm -rf` :
* `man` : help manual
* rmdir : remove folder

website with more command list- http://www.mediacollege.com/linux/command/linux-command.html

# Basic Git commands
* `git init` : create new local repository
* `git add .` : add files
* `git add hello.txt` : add hello text
* `git status` : list the files you've changed and those you still need to add
* `git commit -m 'created hello.txt` : commit change to head - with the note created hello txt
* `git push origin master` : send changes to the master branch of your remote repository

website with more git commands- https://confluence.atlassian.com/bitbucketserver/basic-git-commands-776639767.html